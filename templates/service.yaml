apiVersion: v1
kind: Service
metadata:
  name: {{ include "eversonapp.fullname" . }}
  labels:
    {{- include "eversonapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eversonapp.selectorLabels" . | nindent 4 }}
